const UniverService = require('./univer-service');

test('testing getAll function', () => {
    expect(3).toEqual(3);
});
test('testing update function', () => {
    expect(3).toEqual(3);
});
test('testing delete function', () => {
    expect(3).toEqual(3);
});
test('testing create function', () => {
    expect(3).toEqual(3);
});
test('testing getRating function', () => {
    expect(3).toEqual(3);
});
